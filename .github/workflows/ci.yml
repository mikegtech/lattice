name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  policy:
    name: policy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Policy check
        run: |
          echo "Policy checks passed"
